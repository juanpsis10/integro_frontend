export interface ListPatient {
  noApepat: string;
  noApemat: string;
  noNombres: string;
  noDocide: string;
  flEstado: boolean;
  feNacimiento: Date;
  coAsegurado: string;
  descripcionTipoDocide: string;
}
